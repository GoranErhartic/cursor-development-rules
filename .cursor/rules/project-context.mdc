---
description: "Project-specific routing metadata - active stacks, directory-to-router mapping, key dependencies. Generated by project onboarding; if empty or missing, run development/project-onboarding.mdc."
alwaysApply: true
tags: ["routing", "onboarding", "project-metadata"]
relatedRules: ["development/project-onboarding.mdc", "rule-index.mdc"]
---

# Project Context

This file is generated by **project onboarding**. It gives the agent immediate knowledge of which stack routers apply to this project so rules can be loaded without inferring from open files.

**If the sections below are placeholders or this file was not generated yet:** Request `development/project-onboarding.mdc` and run the onboarding procedure to analyze the project and regenerate this file.

## Project

| Field | Value |
|-------|--------|
| Name | _(filled by onboarding)_ |
| Type | monorepo / monolith / microservices |

## Active Stacks (directory → router)

| Directory / path pattern | Router file |
|--------------------------|-------------|
| _(e.g. `functions/`, `packages/api/`)_ | `languages/aws-lambda/nodejs-lambda.mdc` |
| _(e.g. `frontend/`, `apps/web/`)_ | `languages/react/react-frontend.mdc` |
| _(e.g. `backend/`, `*.java`)_ | `languages/java/java-spring.mdc` |
| _…_ | `languages/nodejs/nodejs-base.mdc` or `languages/csharp/csharp-dotnet.mdc` |

Use this table to choose which router to read when working in a given path. Then load 2–3 rules from that router for the current sub-task.

## Key Dependencies

| Stack / area | Key deps (examples) |
|--------------|---------------------|
| _(filled by onboarding from package.json, pom.xml, etc.)_ | |

## Architecture Notes

_(Brief notes: serverless vs traditional, API style, data stores, event flow.)_
